pipeline {
    agent any
    stages {
        stage('Create Stack into Dev Env') {
            steps {
            sh '''
                set +x
                echo ${DEV_ROLE_ARN}
                '''
              }
             }
            }
            }
